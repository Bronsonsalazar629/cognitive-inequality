# API Keys Configuration
# DO NOT commit this file - use env var DEEPSEEK_API_KEY instead

# LLM Provider Selection
llm_provider: "deepseek"

# DeepSeek API Configuration (unlimited requests, no rate limits)
deepseek_api_key: ""  # Set via DEEPSEEK_API_KEY env var
deepseek_model: "deepseek-chat"

# Configuration
enable_llm: true
enable_cache: true
log_level: "INFO"
